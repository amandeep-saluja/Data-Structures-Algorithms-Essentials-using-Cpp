#include <iostream>
#include <vector>
using namespace std;

int maxProfit(vector<int> wts, vector<int> prices)
{
    return 0;
}

int main()
{

    int n = 4, W = 11;
    vector<int> wts{2, 7, 3, 4};

    vector<int> prices{5, 20, 20, 10};

    cout << maxProfit(wts, prices);

    return 0;
}